---
trigger: always_on
---
# Commander

1. Use `commander` for CLI argument parsing
2. Use `program.command()` for defining commands
3. Use `.option()` for command-line flags
4. Use `.argument()` for positional arguments
5. Provide descriptions for all commands and options
6. Use `.version()` for version information
7. Use `.helpOption()` for help flag
8. Use `.parse()` to process arguments
9. Use action handlers for command logic
10. Validate arguments with custom validators
11. Use subcommands for complex CLIs
12. Handle errors gracefully with try-catch
13. Use `.configureOutput()` for custom output
14. Provide clear error messages
15. Test CLI commands with integration tests
